{
  "name": "mail-creator-ai-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "install:all": "npm install --prefix client && npm install --prefix server",
  "build": "npm run build --prefix client",
  "vercel-build": "npm install --prefix client && npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma && npm run build --prefix client"
}
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-validator": "^7.0.0",
    "groq-sdk": "^1.2.1",
    "jsonwebtoken": "^9.0.0",
    "pdfkit": "^0.19.1",
    "prisma": "^5.22.0"
  }
}
